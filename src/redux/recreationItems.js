import { RECREATIONITEMS } from '../shared/recreationItems';

export const RecreationItems = (state = RECREATIONITEMS, action) => {
    switch (action.type) {
        default:
          return state;
      }
};